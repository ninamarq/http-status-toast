import { styled } from 'styled-components';

const MessageContainer = styled.div`
  display: flex;
  flex-wrap: nowrap;
  box-sizing: border-box;
  padding: 12px 4px;
`;

export const styles = { MessageContainer };
